spring:
  application:
    name: eurka-server

  profiles:
    active: dev

---
spring:
  profiles: dev

logging:
  config: classpath:logback/log4j2-dev.xml

---
spring:
  profiles: sit

logging:
  config: classpath:logback/log4j2-sit.xml